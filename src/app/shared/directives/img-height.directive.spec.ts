import { ImgHeightDirective } from './img-height.directive';

describe('ImgHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgHeightDirective();
    expect(directive).toBeTruthy();
  });
});
